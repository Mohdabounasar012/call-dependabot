name: Call Reusable Workflow

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  call-reusable-workflow:
    uses: Mohdabounasar012/dependabo-reusable/.github/workflows/main.yml@v1.0.3
    with:
      message: "Hello from the caller workflow!"
